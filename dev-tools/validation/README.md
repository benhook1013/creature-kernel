# Documentation validation

This directory contains the repository's lightweight documentation validator,
[validate_docs.py](validate_docs.py), plus a [tests](tests/) directory for
focused validator tests.

## Run

From the repository root:

```bash
python3 dev-tools/validation/validate_docs.py
```

See the [developer-tools overview](../README.md) for the surrounding tooling
context.
