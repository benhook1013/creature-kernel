# CK-KICK-010 tests

These tests cover resolver, geometry, artifact, and CLI behavior, plus the
evidence-reproduction helper.

From the experiment directory, the focused command documented by the parent is:

```bash
python -m pytest -q
```

See the experiment [README](../README.md) for environment setup and the limits
on interpreting these results.
